[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "greek-elite-bot"
version = "1.0.0"
dependencies = [
    "python-telegram-bot==20.7",
    "pandas==2.1.4", 
    "catboost==1.2.5",
    "numpy==1.24.3",
    "schedule==1.2.0"
]